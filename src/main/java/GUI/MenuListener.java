package GUI;

/**
 * Legacy key-based menu listener. Retained only so the file does not get
 * removed; the menu is now button-based inside {@link MainMenu}.
 */
@Deprecated
public class MenuListener {
}
