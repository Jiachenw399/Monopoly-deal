package model;

/**
 * Legacy placeholder. The simplified rules only model rent action cards;
 * the concrete rent data lives on {@link ActionCards} itself. This enum is
 * kept only so old build artefacts keep compiling and is not used by any
 * game logic.
 */
@Deprecated
public enum ActionCardType {
    UNUSED
}
