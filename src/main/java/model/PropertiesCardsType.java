package model;

/**
 * Legacy placeholder. The simplified rules model properties as a flat
 * single-colored card (see {@link PropertiesCards}) and do not use typed
 * wild cards. This enum is kept only so old references compile and is not
 * used by any game logic.
 */
@Deprecated
public enum PropertiesCardsType {
    UNUSED
}
